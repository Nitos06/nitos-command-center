# ─── SECRETS — NEVER COMMIT ───────────────────
state/secrets/
!state/secrets/.env.template
.env
!.env.template
.env.local
.env.*.local
*.key
*.pem
credentials.json
*-tokens.json

# ─── Runtime noise ───────────────────────────
state/_logs/
state/**/runs/
state/**/_cache/
state/**/raw/
*.log
runs/

# ─── OS junk ─────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ─── Editor ──────────────────────────────────
.vscode/
.idea/
*.swp

# ─── Python ──────────────────────────────────
__pycache__/
*.pyc
.venv/
venv/

# ─── Node ────────────────────────────────────
node_modules/

# ─── Next.js ─────────────────────────────────
.next/
out/
build/
.vercel/
next-env.d.ts

# ─── NOTES ───────────────────────────────────
# state/branding/, state/market-research/, state/avatar-builder/, etc.
#   ARE pushed (small JSON, needed on new laptop).
# skills/**/references/my-*.md ARE pushed (the owner's bible — without these, skills are placeholders).
# Repo MUST stay PRIVATE — it contains brand strategy + skill playbooks (competitive IP).
.vercel
